{% extends '/master/default.twig' %}
{% set subtitle = "Lokacje" %}

{% block main %}

    <div class="container">
        <h1>{{ subtitle }}</h1>
        <div class="deck" data-module="locations"></div>
    </div>

{% endblock %}
